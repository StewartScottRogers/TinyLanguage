# Demo: lambda with block body
let classify := function(n : int)
    if n > 0 then
        return "positive"
    else if n < 0 then
        return "negative"
    else
        return "zero"
    end
end
print classify(5)
print classify(-3)
print classify(0)