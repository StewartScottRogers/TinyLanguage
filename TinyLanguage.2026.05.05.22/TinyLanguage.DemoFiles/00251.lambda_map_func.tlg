function mapArr(arr, f)
  let result := []
  let i := 0
  foreach item in arr do
    result[i] := f(item)
    i := i + 1
  end
  return result
end
let nums := [1, 2, 3, 4, 5]
let doubled := mapArr(nums, function(x) x * 2)
foreach item in doubled do
  print item
end
