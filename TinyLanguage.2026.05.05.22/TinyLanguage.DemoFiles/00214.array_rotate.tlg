function rotateLeft(arr, n)
  let size := len(arr)
  let shifts := n % size
  let result := []
  for i := 0 to size - 1 do
    result[i] := arr[(i + shifts) % size]
  end
  return result
end
let arr := [1, 2, 3, 4, 5]
let rotated := rotateLeft(arr, 2)
foreach item in rotated do
  print item
end
