function reverseInPlace(arr)
  let lo := 0
  let hi := len(arr) - 1
  while lo < hi do
    let tmp := arr[lo]
    arr[lo] := arr[hi]
    arr[hi] := tmp
    lo := lo + 1
    hi := hi - 1
  end
end
let a := [1, 2, 3, 4, 5, 6, 7]
reverseInPlace(a)
foreach item in a do
  print item
end
