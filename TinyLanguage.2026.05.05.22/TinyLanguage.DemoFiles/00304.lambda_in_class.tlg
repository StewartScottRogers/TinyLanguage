class Transformer {
  let fn := null
  Constructor(f)
    this.fn := f
  end
  function apply(x)
    return this.fn(x)
  end
}
let doubler := new Transformer(function(x) x * 2)
let squarer := new Transformer(function(x) x * x)
print doubler.apply(5)
print squarer.apply(5)
