class Range {
  let current := 0
  let stop := 0
  let stride := 1
  Constructor(start, s, st)
    this.current := start
    this.stop := s
    this.stride := st
  end
  function hasNext()
    return this.current < this.stop
  end
  function next()
    let v := this.current
    this.current := this.current + this.stride
    return v
  end
}
let r := new Range(0, 10, 2)
while r.hasNext() do
  print r.next()
end
