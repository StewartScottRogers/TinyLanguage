function reverseArray(arr, lo, hi)
  if lo >= hi then
    return
  end
  let temp := arr[lo]
  arr[lo] := arr[hi]
  arr[hi] := temp
  reverseArray(arr, lo + 1, hi - 1)
end
let data := [1, 2, 3, 4, 5]
reverseArray(data, 0, 4)
foreach item in data do
  print item
end
