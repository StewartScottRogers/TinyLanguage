class Box {
  let width := 0
  let height := 0
  Constructor(w, h)
    this.width := w
    this.height := h
  end
  function area()
    return this.width * this.height
  end
}
let b := new Box(4, 5)
print b.area()
