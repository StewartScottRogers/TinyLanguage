namespace TinyLanguage.Lexer
{
    // Stub TokenType enumeration. Phase 1B will replace this with the full
    // set of token types derived from the BNF grammar. The AST nodes only
    // reference TokenType in BinaryOpNode and UnaryOpNode where the
    // operator token is stored.
    public enum TokenType
    {
        Unknown
    }
}
