# Demo: manual string manipulation (character codes)
function charIsLower(ch : string) -> bool
    let code := int(ch)
    return code >= 97 and code <= 122
end
let s := "hello"
let upper := ""
foreach ch in s do
    print ch
end