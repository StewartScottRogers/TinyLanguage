# Demo: clamp a value between min and max
function clamp(val : int, lo : int, hi : int) -> int
    if val < lo then
        return lo
    end
    if val > hi then
        return hi
    end
    return val
end
print clamp(5, 0, 10)
print clamp(-5, 0, 10)
print clamp(15, 0, 10)