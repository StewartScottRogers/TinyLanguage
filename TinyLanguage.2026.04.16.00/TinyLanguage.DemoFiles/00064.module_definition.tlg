# Demo: module definition
module MathUtils {
    function maxVal(a : int, b : int) -> int
        return if a > b then a else b
    end
    function minVal(a : int, b : int) -> int
        return if a < b then a else b
    end
}
print MathUtils.maxVal(10, 20)
print MathUtils.minVal(10, 20)