# Demo: try/catch with typed exception parameter
function riskyOp(x : int) -> int
    if x < 0 then
        throw "negative input"
    end
    return x * 2
end
try
    print riskyOp(5)
    print riskyOp(-1)
catch (e : string)
    print "Caught string error: " + e
end