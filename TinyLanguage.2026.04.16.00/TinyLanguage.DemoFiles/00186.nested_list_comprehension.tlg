# Demo: list comprehension with nested source
let matrix := [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
let firstElements := [row[0] for row in matrix]
foreach elem in firstElements do
    print elem
end
let sums := [row[0] + row[1] for row in matrix]
foreach s in sums do
    print s
end
