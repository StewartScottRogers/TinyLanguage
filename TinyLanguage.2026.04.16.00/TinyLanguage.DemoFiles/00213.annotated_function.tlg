# Demo: annotated function with multiple annotations
@deprecated(since = "1.0", reason = "Use newFunction instead")
function oldFunction(x : int) -> int
    return x * 2
end
@pure
@inline
function newFunction(x : int) -> int
    return x + x
end
print oldFunction(5)
print newFunction(5)
