# Demo: nested try-catch blocks
try
    try
        throw "inner error"
    catch inner
        print "Inner caught: " + str(inner)
        throw "re-thrown"
    end
catch outer
    print "Outer caught: " + str(outer)
end