# Demo: recursive power function
function pow(base : int, exp : int) -> int
    if exp == 0 then
        return 1
    end
    if exp == 1 then
        return base
    end
    return base * pow(base, exp - 1)
end
print pow(2, 10)
print pow(3, 5)
print pow(10, 3)